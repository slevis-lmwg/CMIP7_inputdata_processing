# Carbon Isoptopes (C13 and C14)

We use the CMIP7 c13 dataset with modified metadata to enable the ability to read the dataset using streams.
We use the CMIP7 c14 dataset with modified metadata and mapped to a 360x720 grid to enable the ability to read the dataset using streams.
